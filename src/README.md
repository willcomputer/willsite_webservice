# willsite_webservice


Start the virtual environment 

```
source .venv/bin/activate
```

If it is the first time running the virtual environment, install packages

```
pip install flask flask_cors psutil bcrypt
```

Run the game

```
python3 main.py
```
